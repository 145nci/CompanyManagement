{% extends 'base.html.twig' %}

{% block body %}
    <div class="entry2">
        <div class="count">
            {{ pagination.getTotalItemCount }}
        </div>
        <h1>User list</h1>
        <table class="list">
            <thead>
            <tr>
                {# sorting of properties based on query components #}
                <th>{{ knp_pagination_sortable(pagination, 'Firstname', 'u.firstName') }}</th>
                <th{% if pagination.isSorted('u.firstName') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(pagination, 'Lastname', 'u.lastName') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'Email', ['u.email', 'u.username']) }}</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for user in pagination %}
                <tr {% if loop.index is odd %}class="color"{% endif %}>
                    <td><a href="{{ path('user_show', { 'id': user.id }) }}"><span>{{ user.firstName }}</span></a></td>
                    <td><span>{{ user.lastName }}</span></td>
                    <td><span>{{ user.email }}</span></td>
                    <td>
                        <ul>
                            <li>
                                <a onclick="expandUser('{{ user.id }}')">show</a>
                            </li>
                            <li>
                                <a href="{{ path('user_edit', { 'id': user.id }) }}">edit</a>
                            </li>
                        </ul>
                    </td>
                </tr>
                <tr>
                    <td colspan="4">
                        <div id="user_{{ user.id }}">

                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>

        <ul>
            <li>
                <a href="{{ path('user_new') }}">Create a new entry</a>
            </li>
        </ul>
    </div>


{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        function expandUser(user_id) {

            $.post('{{ path('user_expand') }}',
                    {user_id: user_id},
                    function (response) {
                        $('#user_' + user_id).html(response['view']);
                    }, "json");
        }
    </script>

{% endblock %}
