{% extends 'base.html.twig' %}

{% block body %}
    <div class="entry">
        <h1>User creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('user_list') }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
